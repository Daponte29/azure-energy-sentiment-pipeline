# RECENTLY COMPLETED

- Scaffolded `coke_synapse` in the monorepo (GOAL / CURRENT_TASK / PROJECT_FLOW_CHART).

# NEXT STEPS

- Define the goal + architecture (what data, what Synapse does with it).
- Design the pipeline in PROJECT_FLOW_CHART.drawio.
- Add a Terraform module (`terraform/modules/synapse/`) + `terraform/environments/coke/{dev,prod}`.
