name: Deploy Function (extract_raw_data)

# Deploys the Python extract code to the Azure Function App when the function
# code or the extract_raw_data scripts change on main.
on:
  push:
    branches: [main]
    paths:
      - "energy_sentiment_pipeline/functionapp/**"
      - "energy_sentiment_pipeline/extract_raw_data/**"
      - ".github/workflows/deploy-function.yml"
  workflow_dispatch:

permissions:
  id-token: write   # required for OIDC federation to Azure
  contents: read

env:
  RESOURCE_GROUP: rg-energynews
  FUNCTION_APP: func-energynews-1eelzy

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Azure login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Build deployment package
        run: |
          mkdir -p build
          cp energy_sentiment_pipeline/functionapp/function_app.py energy_sentiment_pipeline/functionapp/host.json energy_sentiment_pipeline/functionapp/requirements.txt build/
          cp energy_sentiment_pipeline/extract_raw_data/fetch_news.py energy_sentiment_pipeline/extract_raw_data/fetch_co2.py energy_sentiment_pipeline/extract_raw_data/storage.py build/
          cd build && zip -r ../package.zip . && cd ..

      - name: Deploy to Azure Functions
        run: |
          az functionapp deployment source config-zip \
            -g "$RESOURCE_GROUP" -n "$FUNCTION_APP" \
            --src package.zip --build-remote true --timeout 600
